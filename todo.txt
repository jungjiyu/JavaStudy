
2. 질문

    : 일단 path 를 다르게 설정하고 f12 로 확인해보면
      : 경로를 루트로 지정 >> 해당 어플리케이션 내부의 모든 파일에서 해당 쿠키를 얻을 수 있음
        : request, response 에서 모두 "/"

      : 경로를 현재 디렉터리로 지정 >> 현재 디렉터리 혹은 현재 디렉터리의 하위 디렉터리에서 해당 쿠키를 얻을 수 있음
        : request 에서는 "현재디렉터리경로" , response 에서는 "/프로젝트"

    : 경로를 지정 안함 >> 현재 디렉터리 혹은 현재 디렉터리의 하위 디렉터리에서 해당 쿠키를 얻을 수 있음
        : request 에서는 "현재디렉터리경로", response 에는 null



    : 근데  쿠키 객체의 생성 당시에는 path 를 "/" 로 설정하고 클라이언트에게 보낸 상황에서, 쿠키 객체를 삭제하기 위하여 age 를 0 으로 바꾸기만 하고 path 를 따로 설정하지 않고 클라이언트에게 다시 보낸 경우에는 쿠키 삭제가 제대로 진행되지 않는이유?
      : 경로를 지정하지 않은 경우 request 에서는 "현재디렉터리" 가 되는데 루트보다 권한(?) 이 적어서 그런거임?
      
