1. 이클립스 톰켓 파일 계층 

2. 질문
  : 상황>> 쿠키 객체의 생성 당시에는 path 를 "/" 로 설정하고 클라이언트에게 보낸 상황에서, 쿠키 객체를 삭제하기 위하여 age 를 0 으로 바꾸기만 하고 path 를 따로 설정하지 않고 클라이언트에게 다시 보낸 경우에는 쿠키 삭제가 제대로 진행되지 않았음.
    : 일단 처음 생성 당시에는 루트로 경로를 설정해서 해당 애플리케이션 내부의 모든 파일들에서 쿠키에 접근 가능하게한거 이해감. 
    : 근데 setMaxAge() 로 유효시간 변경이후 다시 경로를 설정하지 않으면 삭제가 안되는지 모르겠음
      : 
