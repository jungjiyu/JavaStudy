https://inpa.tistory.com/entry/MYSQL-%F0%9F%93%9A-JOIN-%EC%A1%B0%EC%9D%B8-%EA%B7%B8%EB%A6%BC%EC%9C%BC%EB%A1%9C-%EC%95%8C%EA%B8%B0%EC%89%BD%EA%B2%8C-%EC%A0%95%EB%A6%AC


객체지향프로그래밍 OOP
	: https://velog.io/@0sunset0/OOP%EC%9D%98-%EB%84%A4%EA%B0%80%EC%A7%80-%ED%8A%B9%EC%A7%95%EC%B6%94%EC%83%81%ED%99%94%EC%BA%A1%EC%8A%90%ED%99%94%EC%83%81%EC%86%8D%EB%8B%A4%ED%98%95%EC%84%B1
	1. 추상화 : 핵심적인 부분만 보여주고, 자세한 내용은 숨기는 것
		: 추상클래스와 인터페이스로 객체의 공통적인 속성/기능 을 추출하여 정의하고, 구현을 분리한다. 사용할 당시에는 추상클래스와 인터페이스를 활용하여 자세한 사항은 숨기도록 한다.

	2. 상속 : 기존의 클래스를 재활용하여 새로운 클래스를 작성
	3. 다형성 : 어떤 객체의 속성/기능이 상황에 따라 여러 가지 형태를 가질 수 있는 성질
		: 메서드 오버라이딩, 메서드 오버로딩

	4. 캡슐화 :  하나의 캡슐로 만들어 데이터를 외부로부터 보호하되, 필요한 부분만 노출시키는것
		
		: 접근제한자(public , protected, private )  로 노출의 정도를 정하고, : getter setter 로 간접적 접근하게 한다



헷갈리는거 정리
	(1) PK&FK 와 JOIN
	    : PK&FK >> 테이블끼리 관계 맺어두는것
	    : JOIN >> 관계 맺은 테이블을 조회하는 법

	(2) 상속관계와 연관관계
		: 상속관계 >> 데이터베이스 설계에선 직접적으로 존재하지 않지만,  객체 지향 프로그래밍의 상속 개념을 흉내내는 것.
		: 연관관계 >> 1대1 , 1대 다 같은 개념



db사용방법
    : LEV0 ~ 1 까지는 DAO 클래스를 사용

	LEV0. JDBC API 직접 << 가장 저수준
 	    step1. 애플리케이션에서 활용하기 위한, db의 테이블에 대한 클래스를 선언
		ex) 
			public class Member{
				private String memberId;
				private String name;
				. . .
			}

	    step 2. db 접근 가능한 DAO 클래스 선언
		: 내부 메서드로 db에서의 작업을 수행함
			step1. sql문 직접 작성
			step2. jdbc api 로 sql 문 직접 실행
			step3. 반환결과를 직접 애플리케이션의 객체로 맵핑 

		ex ) 
			public class MemberDAO{ 
				public Member find(String memberId ){
					//1. SQL문 직접 작성
					String query = "SELECT MEMBER_ID, NAME FROM MEMBER M WHERE MEMBER_ID = ? "
					pstsmt.setString(1, memberId );


					// 2. JDBC API 로 SQL 문 실행
					ResultSet rs = pstsmt.executeQuery(query);

					// 3. 실행결과를 직접 Memeber 객체로 맵핑 
					String memberId = rs.getString("memberId");
					String name = rs.getString("name");
					Member member = new Member( memberId, name );
				}

			}


	LEV1. sql mapper : ibatis(mybatis) , Jdbc template 등 
		: JDBC API 를 직접 사용하는 것에 비해 코드는 더 단순하지만 여전히 비생산적
	LEV2. ORM Object Relational mapping 프레임워크
		: 객체 지향적 개발


자바로 개발하는 대부분의 어플리케이션은 RDB 를 데이터 저장소로 사용하고, 그를 활용하기 위해 SQL 문을 사용함. 자바로 작성한 애플리케이션은 JDBC API 를 사용해서 SQL 을 db에 전달.

SQL 을 직접 다룰때의 문제점 : 
	(1) 매번 직접  mapper 역할을 해야됨: 애플리케이션의 객체와 db 의 데이터의 구조가 다르기 떄문에 개발자가 중간에서 변환작업( 데이터 받고, 객체에 일일이 매치시켜주고. 객체에서 일일이 데이터 꺼내서, 그걸로 쿼리문 날리고. )을 직접 해줘야된다.
	(2) 유지보수가 어렵다 : 객체에 필드를 추가하면, 자동적으로 이전에 DAO 단에 작성된 쿼리문들까지 수정되는게 아니라 일일이 고쳐줘야된다.
	(3) 진정한 계층 분할이 이뤄지지 않는다. : 물리적으로는 SQL 과 JDBC API 를 데이터 접근 계층에 숨기는데 성공했을지 몰라도, 논리적으로는 엔티티와 아주 강한 "의존"관계를 가지고 있다.
		: 그러니까 , 객체가 어떻게 구성되있건 sql 문에 따라 어떻게 작동될지 갈려서 엔티티를 신뢰할 수 없고, dao 를 일일이 열어 어떤 sql 이 실행되는지 확인해야된다. 
	(4) 엔티티 신뢰 불가 
	(5) sql 의존적 개발


비스지스 요구사항:

도메인 모델: 비즈니스 요구사항을 정의한 것

엔티티 : 비즈니스 요구사항을 모델링한 객체. 도메인 모델을 객체화 한 것(?)



 객체와 rdb의 패러다임 불일치 문제 >> 객체와 관계형db의 지향 목적이 서로 다르고, 각 기능과 표현방법이 달라 생기는 문제들
	: 지속가능한 애플리케이션 개발 위해선 증가하는 복잡성을 제어할 수 있어야됨
	: 객체지향프로그래밍은 추상화, 캡슐화, 정보은닉, 상속, 다형성 등 복잡성 제어에 유용한 다양한 장치를 제공 >> 그래서 대부분 객체지향언어로 개발
	: 일단 도메인 모델을 객체로 모델링하고, 관건은 해당 객체를 어떻게 저장할 것인가?
		: 일단 객체는 필드(속성)와 메서드(기능)를 가지며, 클래스에 정의되어있다. 
		: 객체 기능인 메서드는 클래스에 정의되어있으니, 메서드까지 저장할 필욘 없고 필드값만, 다른 곳에 영구저장시키면 된다.
			: (역)직렬화>>  직렬화된 객체를 검색하는데 어려움이 있기에 이는 거의 불가능하다. 
				직렬화 serialization : 객체를 (다른 환경에서도 사용 가능하도록) 파일로 저장하는 기능
					: 암기 tip >> 다른 곳에서도 사용 가능하게 곧게 직렬로 나열한다. 
				역직렬화: 파일을 (애플리케이션에서 사용하기 위해) 객체로 복구하는 기능

			: rdb >> 데이터 중심 구조화. 집합적 사고
				: 객체지향개념이 따로 없다
				: 객체와 rdb의 지향 목적이 다르다.

	: 구체적인 문제의 종류들
		: 각각 개발자 차원에서 해결을 할 수 있는 것도 없는 것도 있다.

		(1) 상속 >> 공통적으로 존재하는 필드를 물려받는걸로 해결하는 것
			: 객체에는 상속이라는 개념이 있지만, 테이블에는 그 개념이 없다
			: JPA 없이 직접 해결한다고 하면 >> 가능하지만, ㅈㄴ 귀찮
			    step1. 자식과 부모클래스를 따로 만드는데 이때 부모 클래스 쪽에는 자식의 타입을 저장할 컬럼을 정의한다.
				: ex ) 슈퍼타입을 Item , 서브타입을 Album , Movie 라고 하면
abstarct class Item{
	private String itemId;
	private String name;
	pirvate Integer Price;
	private Item DType; // 자식타입 저장할 필드 
}
		
class Album extends Item {
	private String artist;
}

class Movie extends Item {
	private String director;
	private String actor;
}

	


			    step2. 서브타입 객체를 db 에 저장하기 위해 두개의 sql 문을 작성한다 . JDBC API 를 사용해서 부모 객체에서 부모 데이터만 꺼내서 Item 용 insert SQL을 작성하고, 자식 객체에서 자식 데이터만 꺼내서 Album 용 insert SQL 을 작성한다.
					: ex ) Album 객체를 저장한다고 했을 때
						INSERT INTO Item ... 
						INSERT INTO Album ... 

			    step3.  Album 관련 데이터를 db에서 뽑아 활용하기 위해, Item 과 Album 테이블을 join 해서 직접 조회한 다음, Album 객체를 생성한다.
				ex ) 
					SELECT I.* , A.* 
						FROM ITEM I 
						JOIN ALBUM A 
							ON I.ITEM_ID = A.ITEM_ID;
					를 직접 날려야된다

			: JPA 로 해결한다고 하면 << 간결하다
			    step1. 부모엔티티, 자식엔티티 정의 
			    step2. jpa 를 활용하여 자식엔티티 저장 : 단순히 persist(  ) 를 활용해 JPA 에 객체를 저장한다. >> 직접 2개의 쿼리문을 작성할 필요가 없ㄷ
				: JPA 가 알아서 두 개의 SQL 문을 실행해서, 부모 테이블과 자식 테이블에 나누어 저장한다
				ex ) 문제 언급할 때 예시로 들었던 Album 객체를 저장한다고 했을 때
					INSERT INTO Item ... ; 
					INSERT INTO Album .. ;
					이 실행된다.
			

			    step3.  jpa 를 활용하여 저장한 자식을 꺼내서 사용 : 단순히 find(  ) 를 활용해 객체를 조회한다 << 직접 JOIN 쿼리를 날릴 필요가 없다
				: JPA 가 알아서 JOIN SQL 문을 실행해준다
				ex ) 
					SELECT I.* , A.* 
						FROM ITEM I 
						JOIN ALBUM A 
							ON I.ITEM_ID = A.ITEM_ID;
					가 자동으로 실행된다


		(2) 연관관계 >> 객체가 다른 객체를 필드값으로 가지는 것.
			: 객체 vs 테이블
			    객체 >> 참조기반
				: 참조를 통해 다른 객체와 연관관계를 가지고, 참조를 통해 연관된 객체를 조회한다. 이떄 조회는 참조가 있는 방향으로만 가능하다

public class Member{
	Team team; // 테이블로치면 FK 컬럼 
	Long id;
	String username;
}

public class Team{
	Long id; // 테이블로 치면 PK 컬럼
	String name;
}

Member 객체는 Member 객체의 team 필드로 Team 객체를 참조하여 Team 객체와의 관계를 맺고 , 이 필드로 해당 Team 객체에 접근할 수 있다. Team 객체 자체로만은 Member 객체로의 참조가 불가능하다


			    테이블 >> FK 와 PK 기반
				: 외래키(FK)를 사용해서 다른 테이블과 연관관계를 가지고, 조인을 사용해 연관 테이블을 조회한다. 이떄 조회는 PK 가 있는 테이블, FK 가 있는 테이블 양쪽에서 모두 가능하다.
				



					SELECT TEAM.* , MEMBER.* 
						FROM TEAM  
						JOIN MEMBER  
							ON TEAM.ID = MEMBER.ID;
					뿐아니라 

					SELECT TEAM.* , MEMBER.* 
						FROM MEMBER   
						JOIN TEAM
							ON MEMBER.ID = TEAM.ID;

					도 가능하다

테이블의 경우 MEMBER JOIN TEAM 이든 TEAM JOIN MEMBER 든 둘 다 가능하다


			: 객체지향모델링
				: 테이블이 아니라 객체스럽게 모델링하는 것. FK 로 쓰일 필드를 테이블의 PK 컬럼에 대응하는 ID 값을 저장하도록 하는게 아니라 , PK 에 대응하는 객체 자체를 저장하도록 하는 것.
				: 결국 객체지향모델링을 적용하면, 중간 변환 작업( 객체 <---> 테이블 )이 필요
					sol1. 직접 개발자가 일일이 >> 가능하지만, ㅈㄴ 귀찮
					    : 객체 --> 테이블 
						step0. 연관관계 가지도록 객체 생성 및 추기화
						ex ) 
							Team team = new Team( ); // 참조당하는 객체 생성
							. . .
							Member member = new Member( ); // 참조하는 객체 생성
							. . .
							member.setTeam( team ); // 객체간 연관관계 생성


						step1.  참조필드를 가지는 객체의 id 필드로 PK 값 얻어낸다.
						ex ) Long pk = member.getId( ); 

						step2.  참조필드를 가지는 객체의 참조필드에서 참조당하는객체를 뽑고, 그 객체애서 id 값을 또 뽑아서 FK 값 얻어낸다. 
						ex ) Long fk = member.getTeam( ).getId( );

					    : 테이블 --> 객체
						step1. 참조를하는테이블과 참조당하는테이블을 join 해 얻어옴.
						ex ) SELECT TEAM.* , MEMBER.* 
								FROM MEMBER   
								JOIN TEAM  
								ON MEMBER.ID = TEAM.ID;

						step2. 쿼리 결과에서 데이터 뽑아서 참조당하는 객체 생성 및 초기화

						ex )
							 Team team = new Team( );
							 team.setId( 쿼리결과에서추출한아이디값 );
							 team.setName( 쿼리결과에서추출한이름값 );

						step3. 쿼리 결과에서 데이터 뽑아서 참조하는 객체 생성 및 앞서 생성한 참조당하는 객체도 활용하여 초기화

						ex ) 
							 Member member = new Member( );
							 member.setId( 쿼리에서추출한아이디값);
							 member.setTeam( 앞서생성한Team객체 );

	


					sol2. JPA 사용 >> ㅈㄴ 좋음
					    : 객체 --> 테이블 
						step0. 연관관계 가지도록 객체 생성 및 초기화
							ex) 이건 sol1 과 같음
						step1. 참조하는객체를 걍 persist( )로 저장
							: 이케하면 JPA 가 알아서 참조필드를 FK 로 변환해서 적절한 insert 문을 작성해준다. 
							ex ) jpa.persist( member );
 
					    : 테이블 --> 객체
						step1. 참조하는객체를 걍 find( ) 로 조회
							: 이케하면 JPA 가 알아서 FK 컬럼을 참조필드로 변환해 객체 생성시켜준다.
							ex ) Member member = jpa.find( Member.class , memberId );

								
			
		(2) 객체 그래프 탐색 >> 객체끼리 가지는 참조 관계로 , 건너 건너 다른 객체를 얻는 것.
			: 일차원적으로 



ORM Object Relational mapping 프레임워크
	:  관계형 데이터베이스 와 객체 간의 차이를 중간해서 해결




JPA Java Persistence API 
	: 자바 진영 ORM 기술 표준
	: 객체 중심 개발을 가능하게 해줌 >> 생산성/유지보수 증가
	: CRUD SQL 문을 개발자가 직접 작성하지 않아도 됨	
		: 따로 네이티브 sql 문을 활용해서 직접 sql 문을 작성할 수도 있음
	: db시스템 에 종속되지 않고 개발 가능 ( 한 코드를 mysql 에서 돌릴수도 oracle 에서 돌릴수도 있음)
	: JPA 에서의 "매핑정보" >> 어떤 객체를 어떤 테이블에 관리할지 정의한 정보
	: JPA 가 제공하는 API 를 사용한다
		(1) 저장 : .persist( 엔티티객체 )
 			:  JPA 가 객체와 매핑정보를 보고, 적절한 insert SQL 문을 생성하여 db 에 전달한다
				ex ) 
					Member member = new Member("jojo123" , "jojo");
					jpa.perisit(member); // 생성

		(2) 조회 : .find( 엔티티클래스명.class , 엔티티의특정필드값 )
			: JPA 는 객체와 매핑정보를 보고 적절한 select SQL 문을 생성해서 db에 전달하고, 그 결과로 해당 엔티티타입의 객체를 생성하여 반환한다.
				ex ) 
					String memberId = "jojo123"; 
					Member member = jpa.find( Member.class , memberId );  // 조회

	
		(3) 수정 : 별도의 메서드 X
			: JPA는 별도의 수정 메서드를 제공하지 않는다. "조회( find )해서 얻은" 객체를 수정하면 트랜잭션을 commit 할 때, db에 적절한 update SQL 문이 전달된다.  << 3장 자세히
				ex ) 
					Member member = jpa.find( Member.class , memberId ); 
					member.setName("johojoho"); // 수정 

		(4) 연관된 객체 조회  : 별도의 메서드 X
			: 연관된 객체를 "사용"하는 시점에 적절한 select SQL 문을 실행한다. << 8장자세히
				ex ) 
					Member member = jpa.find( Member.class , memberId ); 
					Team team = member.getTeam( ) ; // 연관객체조회


 
