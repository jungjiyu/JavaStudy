빌더 패턴 >> (생성자를 통한 객체 생성이 아닌) 메서드 체이닝으로 객체 생성을 구현 하는 것.
	: (생성자패턴보다) 유지보수성 좋고 직관성 좋음
	: https://stir.tistory.com/343

------------------------------------------------------------------------------------------------------------------
: REST REpresentational State Transfer >>  자원을 이름으로 구분하여 해당 자원의 상태를 주고받는 모든 것
	: 자원을 이름(URI)로 구분하여 HTTP Method(POST, GET, PUT, DELETE)를 통해 해당 자원에 대한 CRUD Operation을 적용하는 것
	: representational == 표현적인.  
	: state == 상태
	: transfer == 전송



SSR, CSR
	SSR Server Side Rendering >> 서버에서 페이지를 모두 구성하여 사용자에게 response 하는 방식
		: 벡엔드 분야에서 사용
		: 과정
			1. 서버가 이미 렌더된 인덱스 파일을 response 하고, 클라이언트가 이를 다운 받음
				: 볼 수는 있지만 Viewable 상호 작용 Interactable 은 아직 불가
			2 클라이언트가 JS 를 다운 받음
				: 이제 상호작용 가능


	CSR Client SIde Rendering >> 서버가 아닌 클라이언트 사이드에서 웹 페이지를 렌더링하는 기술
		: 프론트 분야에서 사용
		: 과정
			1. 서버가 인덱스 파일을 response 하고, 클라이언트가 다운 받음
				: 여기엔 내용이 없어 아무것도 보지 못한다
				
			2. 받아온 자바 스크립트 링크로 자바스크립트를 서버에 요청하여 클라이언트 로직과 렌더링 코드를 받아온다
			3. 자바스크립트로 결과를 렌더링하여 Viewable and Interactable 해진다




자바스크립트 Promise 개념
	: 비동기 처리 >> 현재 실행중인 작업과 별도로( 병렬적으로 ) 다른 작업을 수행하는 것,
	: 콜백 함수 >> 비동기 작업이 완료되면 호출되는 함수. 비동기 함수의 매개변수로 함수 객체를 넘기는 기법. 
		: 중첩되면 개복잡 해진다. 콜백 지옥.

		function increaseAndPrint(n, callback) {
		  setTimeout(() => {
 		   const increased = n + 1;
  		  console.log(increased);
  		  if (callback) {
  		    callback(increased); // 콜백함수 호출
  		  }
 		 }, 1000);
		}

		increaseAndPrint(0, n => {
		  increaseAndPrint(n, n => {
  		  increaseAndPrint(n, n => {
  		    increaseAndPrint(n, n => {
    		    increaseAndPrint(n, n => {
   		       console.log('끝!');
  		      });
 		     });
 		   });
		  });
		});
	
	: Promise 객체 >> 비동기 작업의 최종완료||실패 결과 를 나타내는 객체
		: 비동기 작업의 결과를 반드시 반환하겠단 의미서 Promise 라 이름 지어졌다
		: 콜백 지옥을 벗어나게 해준다
		: 비동기 작업 갯수가 많아져도 코드가 깊어지지 않는다
		: Promise 객체 생성 방법 
			const myPromise = new Promise((resolve, reject) => {
				const data = fetch('요청할URL');

   				 if(data) resolve(data); // 요청 성공하여 데이터가 있다면
 				 else reject("Error");
			})

				: arg 로 들어가는 콜백함수의 0번째 인수는 성공(resolve) 를 나타내는 객체, 1번째 인수는 실패(reject) 를 알려주는 객체.
					: Promise 생성자 안에 들어가는 콜백함수를 executor 라고 부른다
				: fetch 의 결과로 데이터가 있다면 if 문이 실행되어 resolve 함수가, 그렇지 않다면 reject 함수가 실행된다. 이때 resolve 함수의 경우 arg 로 data, 즉 fetch( ) 의 결과를 넣어준다

		
		: Promise 객체 처리 방법 
			: 생성된 Promise 객체를 이용하여 비동기 작업 완료 이후에 다음 작업을 연결시켜, 체인처럼 , 진행할 수 있다
				: 줄줄이 이어 쓰는걸 체이닝이라고 한다
			: 프로미스 핸들러  << promise 객체 활용하는 메서드들 
				then( (value) => { 실행내용 .. } )  --> 비동기작업성공했을 경우 실행되고, 새로운 Promise 객체를 반환한다
					: resolve 에서 fetch( ) 의 결괏값을 arg로 주기 떄문에 then 내부의 함수 객체에서 해당 결괏값을 활용 가능하다 ( then 내부서 받은 파라미터 변수명으로 )
				catch( ( error ) => { 실행내용 .. } ) --> 비동기 작업 실패했을경우 실행되고, 새로운 Promise 객체를 반환한다

				
				fianlly( ( ) => { 실행내용 }  ) --> 무조건 실행시키고, 새로운 Promise 객체를 반환한다.

		: promise 객체 state  ) Pending - Fulfilled - Ejected 
			Pending >> 대기 상태. 처리가 완료되지 않은, 진행중인 상태
			Fullfilled >> 성공적으로 처리가 완료된 상태
			Ejected >> 처리가 실패적으로 끝난 상태

		: promise 객체는 보통 코드 자체에서 직접 생성 안하고, 함수를 만들고 반환하게 하여 사용하는 편이다
			: 결국 " 함수명( ).then( 어쩌구 ).then( 어쩌구 ) ... " 이런식으로 쓰게 되는거다
			: 프로미스 객체를 생성하여 반환하는 이러한 함수를 "프로미스 팩토리 함수" 라고 부른다
			: 대표적으로 fetch( ) 함수가 "프로미스 팩토리 함수" 이다
				fetch( "요청경로" ) // response 객체 반환 
 				.then((response) => response.json()) // response 객체 받고 JSON 데이터 반환
  				.then((data) => console.log(data)); // 받은 JSON 데이터를 출력

		: 여러개의 비동기 처리를 한번에 하는 법 >>
			: 공통적으로 fetch( ) 를 요소로 하는 배열을 Promies.all(  )메서드의 arg 로 한다

			1.  Promise.all( [ fetch( "경로1 " ) ,fetch( "경로2 " ) , ... ]  ) 
				: all( ) 내부의 "모든" 비동기 처리가 완료되어야 이 뒤의 then( ), catch( )로 넘어간다.

			2. Promise.any(  [ fetch( "경로1 " ) ,fetch( "경로2 " ) , ... ]  ) 
				: all( ) 내부의  비동기 처리 중 하나라도 "성공적으로" 완료되면 바로 뒤의 then( ), catch( )로 넘어간다.
					: 그러니까 아무리 처음에 반환된 결과라도 그게 실패이면 무시되는거다
					: 모두 성공적이지 않을 시 최종적으론 실패 결과를 반환
		
			3. Promise.race(  [ fetch( "경로1 " ) ,fetch( "경로2 " ) , ... ]  ) 
				: 비동기 처리중 젤 먼저 완료된 것을 반환하고, 다음 then( ) , catch( ) 로 간다	
				: any 와 다르게 성공 실패 여부가 중요하지 않다


	: https://inpa.tistory.com/entry/JS-%F0%9F%93%9A-%EB%B9%84%EB%8F%99%EA%B8%B0%EC%B2%98%EB%A6%AC-Promise#%EC%BD%9C%EB%B0%B1_%EC%A7%80%EC%98%A5%EC%9D%84_%ED%83%88%EC%B6%9C%ED%95%98%EB%8A%94_%EC%83%88%EB%A1%9C%EC%9A%B4_%EB%AC%B8%EB%B2%95


------------------------------------------------------------------------------------------------------------------


XML eXtensible Markup Language : 사용자 정의형 HTML


JSON JavaScript Object Notation >> 데이터의 교환/저장을 쉽게 해주는 텍스트 기반의 데이터 교환 표준.(표현법)
	: 직역하면 자바 스크립트 객체 표기법
		: Notation == 표기법
		: Javascript에서 객체를 만들 때 사용하는 표현식을 사용한다
	

	: 문법		
		: object >> key 와 value 로 구성된 정렬되지 않은 property( 속성 ) 의 집합
			: 객체 생성 형식 >> javascript 랑 표현 방식이 같다
				{
					"키명1" : 값1,
					"키명2" : 값2 ,
					. . . 
					"키명n" : 값n
				}

				: key>> 문자열이라 항상 " " 로 표기한다.
				: value >> 문자열일 수도 있고 숫자일수도 있으므로 알잘딱깔센
				: key 와 vlaue 는 ':' 로 바인딩하고, 여러 데이터를 나열할 경우 ',' 를 사용한다
				: 최종적으론 중괄호 로 묶는다.

			: 객체는 다른 객체의 property 값일 수 있다
				ex ) 	{"판매자정보" : { "이름" : "남도일", "지역" : "서울" } , "판매품목" : ['사과','배','딸기']  }

			: 활용 형식
				sol1. 배열적 표현 >> 객체명[ "속성명" ]
				sol2. 객체적 표현 >> 객체명.속성명



		: 배열 array
			: 형식 >>  [ 값1 , 값2 , 값3 .. ] 
				: 대괄호로 감싼다
				: ex ) var Characters = [
    					{ '이름' : '남도일',  '성별' : '남',   '나이' : 18 },
    					{ '이름' : '유미란',  '성별' : '여', '나이' : 17 } ]

			: 요소 추가 방법 >> 배열명.push( 값 )
				ex ) var Character = { '이름' : '장미란',  '성별' : '남',   '나이' : 40 },
				     Characters.push(Character);
	 
			: 객체를 요소로 할 때 , 각 객체마다 속성 추가 방법
				sol1. 배열명.새항목명 = 값;
					ex ) Character.성인여부 = true;

				sol2. 배열명["새항목명"] = 값;
					ex ) Character["성인여부"] = true;
					ex ) var key ="성인여부";
						Character[key] = true;

			: 객체를 요소로 할 때 , 각 객체의 속성 삭제 방법 >> delete 배열명.속성명
				ex ) delete Character.직접잡은범인
			
		: value 의 타입
			1. 숫자 number  --> 쌩 숫자
			2. 문자열 string --> "쏼라쏼라" 혹은 '쏼라쏼라'
			3. 불리언 boolean ---> true , false 
			4. 객체 object
			5. 배열 array 
			6. null	


		: 제공 메서드 
			1. JSON.stringfy ( 변수 ) >> 직렬화. 객체를 문자열로 변환해준다
				ex )	 var str = {"test" : "value"}
					var parsingData = JSON.parse(str); //  '{ "test" : "value" }' 

			2. JSON.parse( 변수 ) >> 역직렬화. 문자열을 객체로 변환해준다 
				ex )	 var str = '{ "test" : "value" }';
					var parsingData = JSON.parse(str); // {"test" : "value"}

	: https://codingazua.tistory.com/4
	: https://lxxyeon.tistory.com/153


------------------------------------------------------------------------------------------------------------------

HTTP 메세지
	: 요청할 때는 HTTP request Message, 응답할때는 HTTP respnose Message 에 데이터가 실려 전송된다
	: HTTP request 메세지 구성
		1. 요청 라인 (시작라인) >> 요청방식 URL HTTP버전
			ex ) GET /posts/1/comments HTTP/1.1

		2. 헤더 header >> HTTP 전송에 필요한 metadata 가 있다
			ex )  Content-type : application/json
			      Host: jsonplaceholder.typicode.com
				:  jsonplaceholder.typicode.com (서버) 로 request 보내겠다.

		3. 본문 body >> 실제(본질적인) 데이터.
			ex ) 
			{ 
				"title" : "미미미미누",
				"body" : "유튜버"
			}

	: HTTP response 메세지 구성
		1. 상태 라인(시작라인) >> HTTP버전 상태코드
			 ex )   HTTP/1.1 200
				: HTTP/1.1 버전을 사용하여 response 하였고, 성공적으로 처리 되었다

		2. 헤더 header >> HTTP 전송에 필요한 metadata 가 있다
			ex) date: TUE , 30, Nov 2022 06:  .. 
			     content-type: application/json

		3. . 본문 body >> 실제(본질적인) 데이터.
			: REST API 에서는 HTTP response 표준을 JSON 으로 한다
			ex ) 
			{ 
				"title" : "미미미미누",
				"body" : "유튜버",
				"id":2
			}

 

------------------------------------------------------------------------------------------------------------------



------------------------------------------------------------------------------------------------------------------
REST API 	>> HTTP URL 로 서버의 resource를 명시하고, HTTP 메서드로 해당 자원을 CRUD 할 수 있게 하는 인터페이스. 
	: REST 기반 API
	: 효과 >> 서버의 자원을 클라이언트에 구애받지 않고 사용할 수 있게한다 . 즉 ,  HTTP request 대한 response 로 "특정기기에 종속되지 않는 서버의 자원"을 반환한다.
		: view 가 아닌 data 를 response 하고, 이때 응답되는 데이터가 JSON , XML 이다
			: (사람과 기계 모두 이해하기 쉬우며 용량이 작아서) 요즘엔 JSON 으로 통일되는 추세
		: 클라이언트는 웹 브라우저만 있는게 아니라 스마트폰 , 스마트워치 ... 등 다양하고, 서버는 이러한 다양한 클라이언트로부터의 요청에 응답해야한다. 

	: REST 는 소프트웨어 아키텍쳐이고, HTTP 는 웹 환경에서 정보 송수신할 때 사용하는 규약이다. 반드시 HTTP 를 사용할 필요는 없지만 대부분의 웹 환경이 HTTP 를 사용하기 떄문에 REST 는 대부분 HTTP 기반.

	: 모든 HTTP 메서드를 지원한다.
		: 기본 형태
			(1) fetch('요청URL', 옵션객체)
				: fetch >> HTTP request 전송 기능을 제공하는 클라이언트 사이드 web API . 즉 , 서버와의 통신을 통해 CRUD 를 구현할 수 있게 하는 함수
				: 브라우저에 내장되어있다.
				: 파라미터 
					: ' 경로 ' >> HTTP request 할 URL
					: 옵션객체 >> { method , headers, body } 
						: 필요 없으면 생략 가능
						: 주의 ) 요청 코드 자체는 json 이 아니라 자바스크립트이므로 자바스크립트의 표현법을 따른다. 따라서 객체의 속성은 문자열( 이건 JSON 의 객체에서 key 표현법 )이 아닌 걍글자(?) 로 표현한다.
						: method >> HTTP 요청 메서드
							: 'HTTP 요청 메서드' 를 할당
							ex) method='post'

						: headers >> HTTP 헤더 
							: "Content-Type": "application/json" 
								:JSON 형식으로 데이터를 보내달란 뜻

						: body >> payload 작성
							:  아래에서 "data" 가 실질적인 데이터 즉 payload
 							{ "status" : "OK"
  							"from": "localhost",
  							"to": "https://hanamon.kr/users/1",
  							"method": "GET",
 							 "data":{ "username" : "하나몬" }
							}

				: 반환 >> HTTP 응답을 나타내는 response 객체를 wrapping 한 객체 반환
					: Response 객체 >> HTTP response status/header/body 등을 읽어올 수 있다	
					: Response.ok == HTTP 응답 성공 여부를 나타내는 속성
						true = 200 (응답 성공)
						false = not (응답 실패)
				
					: Response.status = 서버가 어떻게 response 했는지 알려줌
						상태코드 클래스
							1XX >> Informational ( 처리중 )
							2XX	>> SUCCESS (정상처리)
							3XX >> Redirection (추가 request 필요)
							4XX >> Clinet Error ( 잘못된 request )
							5XX >> Server Error (서버측에서 처리하다 에러남)			
					: Response.prototype
						: Response 객체의 활용 위한 다양한 메서드 제공
						: Response.prototype.json 메서드 >> Response 객체에서 HTTP BODY ( response.body) 를 취득해 역직렬화 ( JSON 포맷 문자열을 객체화 )

			(2) .then( (response) => response.json( ) ) 
				: response는 HTTP 응답을 나타내는 Response 객체.
				: json( ) 메서드를 활용해 역직렬화 한다

			(3) .then((json) => console.log(json)) 
				: json 은 역직렬화된 HTTP response Body

			: https://velog.io/@seul06/fetch-API
			: https://velog.io/@dev-redo/REST-API%EC%99%80-Fetch-api



		: REST API 의 구성 3요소
			(1) resource : 자원 --> URL 로 표현
				: URL 은 되도록 동사형이 아닌 명사형을 쓰는게 좋다
					ex ) GET /getTodos/1 <-- 좋지 않다
					      GET /todos/1 <-- 좋다

			(2) verb : 자원에 대한행위 --> HTTP Request method 로 표현
				:  HTTP Request method 는 밑에 자세히 적음

			(3) representation : 자원에 대한 행위의 구체적 표현.내용  --> payload 로 표현


	: HTTP request method >> 클라이언트가 서버에게 request 종류와 목적(리소스에 대한 행위)를 알리는 방법


		1. Read --> HTTP get 메서드 
			: sql 문으로는 select
			: 페이로드 X
			: 형식 
				fetch('URL')
				.then(( response ) => response.json)
				.then(( json ) => console.log(json));

				: method 속성의 default 값은 get 이므로 get 쓸 떄는 method 속성 생략 가능하다

		

		2. Create --> HTTP post 메서드
			: sql 문으로는 insert
			: 페이로드 O
			: 형식
				fetch("https://jsonplaceholder.typicode.com/posts", {
				method: "POST",
  				headers: {
 				   "Content-Type": "application/json",
 				 },
  				body: JSON.stringify({
  				  title: "Test",
  				  body: "I am testing!",
  				  userId: 1,
  				}),
				})
 				 .then((response) => response.json())
 				 .then((data) => console.log(data));

				: 
		
		3. Update --> HTTP put 혹은 patch 메서드
			: sql 문서 update 
			: put >> 기존 데이터를 전체 변경 . 만약 기존 데이터가 없다면 새로 생성
				: 페이로드 O

			: patch >> 기존데이터를 부분적 변경
				: patch 라는게 작은 조각 뭐 이런 뜻이다
				: 페이로드 O

		4. Delete --> delete
			: sql 문서 delete
			: 페이로드 X




	:  HTTP 메서드 연습
		 >> place holder ( https://jsonplaceholder.typicode.com/ )
			: 가짜 API. 서버 역할을 해준다
			: 모든 게시글 조회 ex) 	/posts  
			: 특정 게시글 조회 ex)	/posts/1

		 >> Talend API Tester 크롬 확장 프로그램 
			: request 보내게 해준다 
			: 밑에 HTTP 탭을 택하면 HTTP request/response message 의 내용 볼 수 있다


	












	: Talend API Test 로 확인해보기 >> 메서드를 택하고 URL 을 "http://localhost:8080/특정위치" 로 기재하여 send 한다.
		: 참고로 일반 @Controller 에 매핑되어있는 URL 로도 request 가능하고, 이 경우 HTML 과 같은 페이지 가 리턴 되는 것을 알수 있따


	: REST 컨트롤러
		: REST API 로 request 받고, response 가능한 컨트롤러로, JSON 을 반환한다.
		: REST 컨트롤러에선 일반 컨트롤러에서 사용 가능하던 어노테이션 모두 사용 가능하고, 여기에 + α 로 몇 개 더 사용 가능하다.


		: Http response message 의 body 에 담길 데이터를 설정하는 방법
			: ResponseEntity<T>  는 부가적인 데이터까지 포함할 수 있어 사용이 지향된다
			sol1.  단순 엔티티 타입 객체 반환 >> response body 만 반환		
				: @ResponseStatus 를 사용하여 상태코드도 같이 전송할 순 있으나 "헤더" 는 반환 못한다.
			**** sol2. ResponseEntity<T> 반환 >> response body + 상태코드 + 헤더 반환

			: https://ksh-coding.tistory.com/89


		HTTPStatus
			: HTTP 상태코드를 관리하는 클래스로, 다양한 ENUM 타입 관련 메서드를 가짐.
				: ENUM 타입 >> 열거형. 여러 상수로 이뤄진 고정 집합.
					: 추후 더 정리하겠음
					시용법 >> ENUM타입명.상수명
			: 종류
				1. HTTPStatus.OK == 200
				2. HTTPStatus.CREATED == 201
				3. HTTPStatus.BAD_REQUEST == 400



	
		HttpEntity<T>
			: HTTP Request, HTTP Response 에 대응하는 HttpHeader , HttpBody 를 포함하는 클래스
				public class HttpEntity<T> {

					private final HttpHeaders headers;

					@Nullable
					private final T body;

					. . . 
		
				}

			: ResponseEntity<T> , RequestEntity<T> 가 이를 상속한다.


		ResponseEntity<T>
			: REST API 기반 reuqest  의 응답을 위해 사용하는 클래스
			: HttpHeaders, HttpBody 와 HttpStatus을 포함한다.
			: HttpEntity 를 extends 한다.
			: 객체 생성 방법
				sol1. 생성자
					: 결론적으론 (1) , (2) 의 경우  arg 로 들어가지 않은 얘들은 다 null 로 간주해 내부적으로 다 있는 생성자( (3) )을  this( X , X, X ) 로 호출한다. 
					(1) HttpStatus 만 받는 생성자
						public ResponseEntity(HttpStatus status) 

					(2) body  (T 타입 객체)와 HttpStatus 받는 생성자
						public ResponseEntity(@Nullable T body, HttpStatus status) 

					(3) HttpHeaders, HttpBody 와 HttpStatus 다 있는 생성자
  						public ResponseEntity(@Nullable T body, @Nullable MultiValueMap<String, String> headers, HttpStatusCode statusCode)    					 


			*******sol2. 빌더 패턴>>
				: https://00h0.tistory.com/88
				: https://velog.io/@injoon2019/%EC%8A%A4%ED%94%84%EB%A7%81-ResponseEntity

				: ResponseEntity 를 대상으로 하여 필요에 따라 조합해서 사용하면 된다
				*******: build( ) 혹은 body( 내용 ) 은 반드시 포함해야된다
						: build( ) ,  body( 내용 )이 ResponseEntity 객체를 생성하는 기능이 있기 때문
					ex) ResponseEntity.status( HTTPStatus.OK ).body( 내용 ).header( 내용 )
			
   
				(1) static ok( ) >> 상태를 HttpStatus.OK 로 설정한다
    				(2) static ok(@Nullable T body) >> 상태를 HttpStatus.OK 로 설정 및 body 에 담을 내용도 함께 설정한다.
					: ok( ) 의 경우 성공적인 상태로 body 에 담을 내용이 있으니까 오버로딩 되어있는 거지 badRequest, notFound 는 딱히 body 를 담을 필요가 없으므로 메서드가 오버로딩 되어있지 않은 것이다

				(2) static badRequest( ) >>상태를 HttpStatus.BAD_REQUEST 로 설정한다 
				(3) static notFound( ) >>상태를 HttpStatus.NOT_FOUND 로 설정한다 

				(4) static status( HttpStatus status )  >> 상태코드를 arg로 명시한다. 
					: 숫자코드 와 enum ( HttpStatus.OK  같은 )  모두 가능



				(5)  body( 담을내용 )  
					: ResponseEntity 객체를 생성하고 body 에 담을 내용을 설정 헤준다
					: https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/http/ResponseEntity.BodyBuilder.html

     				   public <T> ResponseEntity<T> body(@Nullable T body) {
        				    return new ResponseEntity(body, this.headers, this.statusCode);
       				 }


				(6) static build( ) 
					: body 에 담을 내용이 없을 떄 사용한다. body 의 값을 null 로 설정해준다. 
						: body( 내용 ) 이 결국 ResponseEntity 객체 생성하는 거라 얘도 결국 ResponseEntity를 생성하는 역할을 하게 된다.

					: https://00h0.tistory.com/88
					 public <T> ResponseEntity<T> build() {
   					          return this.body((Object)null);
   					 }

				(6) static created( URI location ) 
					: Http response 메세지의 header 부분에 표시할 location 키값의 value 를 설정할 수 있게 한다
	  				  public static BodyBuilder created(URI location) {
  					      return (BodyBuilder)status(HttpStatus.CREATED).location(location);
   					 }

				(7) header( 담을내용 )
     				   public BodyBuilder header(String headerName, String... headerValues) {
        				String[] var3 = headerValues;
        				int var4 = headerValues.length;
        				for(int var5 = 0; var5 < var4; ++var5) {
        					String headerValue = var3[var5];
        					this.headers.add(headerName, headerValue);
        				}
            				        return this;
            				    }







		: 구현 
			0. api 패키지 생성 
			1. 클래스 만들고 @RestController 를 위에 적음 
				: 클래스명은 주로 XXXXApiController.java
				: @Controller 가 아님을 주의
				: @RestController
					: 일반적인 @Controller 기능 + 내부의 모든 메서드에 자동으로 @ResponseBody 를 추가해줌.

			2. 메서드 생성
				(0) URL 맵핑  >> @GetMapping, @PostMapping 뿐 아니라 @PatchMapping , @PutMapping 도 이제 할 수 있다

				(1) 메서드 위에 사용할 어노테이션 명시 
					@ResponseBody
						: 메서드에서 리턴되는 자바객체를 HTTP response 메세지의 body 에 담아 전송시켜준다.
						: @RestController 에서는 자동적으로 내부 메서드에 @ResponseBody 가 붙기 때문에 생략해도 괜찮긴 하다. 
						: https://velog.io/@nomonday/Spring-RequestBody-ResponseBody-%EC%9D%B4%ED%95%B4%ED%95%98%EA%B8%B0


				(2) 파라미터 설정 >> 
					: 참고로 @PathVariable 은 여기서도 유용하게 사용하지만, @ModelAttribute 는 그닥 사용 안한다(JSON 으로 데이터 주고받으니까)
					@RequestBody
						: http request body 부분을 그대로 전달받고 해당 파라미터 객체에 맵핑해준다
						: json 기반 request 데이터를 받을 경우 @ModelAttribute 이 아니라 @RequestBody 를 쓴다							
						: 주의  	
							0. @ResponseBody 랑 다른거다
							1. @ModelAttribute 과는 다르게 @RequestBody 를 생략한다고 해서 그 파라미터 객체에 자동으로 @RequestBody 가 달리진 않는다. 직접 명시해줘야 json 데이터를 받을 수 있다.
						: https://cheershennah.tistory.com/179

				

			******(3) (뷰네임이 아닌) 데이터를 리턴
					: GET 메서드 
						단일 레코드 정보 >> 엔티티 타입의 객체 
						레코드 집합 정보 >> List<엔티티타입> 객체

					: POST 메서드 >> 엔티티타입 객체

					: PATCH 메서드 >> ResponseEntity< 엔티티타입> 객체
				
												

			








------------------------------------------------------------------------------------------------------------------

